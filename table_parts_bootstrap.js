// Bootstraps the global parts array the generated files append into.
// Keeping this separate lets the "manual" table be split across multiple JS files.
window.DUMB_ADD_TABLE_PARTS = window.DUMB_ADD_TABLE_PARTS || [];
